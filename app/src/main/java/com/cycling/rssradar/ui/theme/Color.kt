package com.cycling.rssradar.ui.theme

import androidx.compose.ui.graphics.Color

// —— 设计稿色板（深色主题） ——

/** 纯黑背景，与设计稿一致。 */
val BgRoot = Color(0xFF000000)

/** 卡片 / 一级容器表面：iOS Dark 风。 */
val Surface1 = Color(0xFF1C1C1E)

/** Tab 选中态等次级容器。 */
val Surface2 = Color(0xFF2C2C2E)

/** Hover / 描边弱化。 */
val Surface3 = Color(0xFF3A3A3C)

/** 紫色强调：选中态、按钮、未读指示、tab 背景。 */
val Accent = Color(0xFF7B7CFF)
val AccentPressed = Color(0xFF6B6CFF)
val OnAccent = Color(0xFFFFFFFF)

/** 链接 / 标题选中色。 */
val Link = Color(0xFF9B9CFF)

/** 主要文字。 */
val TextPrimary = Color(0xFFFFFFFF)

/** 次级文字（摘要、来源）。 */
val TextSecondary = Color(0xFFB0B0B6)

/** 三级文字（时间、提示）。 */
val TextTertiary = Color(0xFF7E7E86)

/** 分割线。 */
val Divider = Color(0xFF2A2A2D)

/** 成功 / 校验通过。 */
val Success = Color(0xFF34D399)

/** 警告。 */
val Warning = Color(0xFFFBBF24)
